{% extends 'AppBundle::admin.html.twig' %}

{% block content -%}
    <h1>State list</h1>

    <table class="table table-bordered records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>State</th>
                <th>Country</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('youbrush_appbundle_admin_state_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.country }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('youbrush_appbundle_admin_state_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('youbrush_appbundle_admin_state_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('youbrush_appbundle_admin_state_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
