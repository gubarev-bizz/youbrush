{% extends 'AppBundle::admin.html.twig' %}

{% block content -%}
    <h1>Skills</h1>

    <table class="table-bordered table record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('youbrush_appbundle_admin_skills') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('youbrush_appbundle_admin_skills_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
