{% extends 'AppBundle::admin.html.twig' %}

{% block content -%}
    <h1>City list</h1>

    <table class="table-bordered table records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>City</th>
                <th>State</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('youbrush_appbundle_admin_city_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.state }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('youbrush_appbundle_admin_city_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('youbrush_appbundle_admin_city_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('youbrush_appbundle_admin_city_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
